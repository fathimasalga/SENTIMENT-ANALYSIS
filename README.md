# SENTIMENT-ANALYSIS
A machine learning-based Sentiment Analysis project that classifies emotions in text using TF-IDF feature extraction and two models: Naive Bayes and Support Vector Machine (SVM). The project includes text preprocessing, feature engineering, model evaluation (Accuracy &amp; F1-score), and performance comparison.
